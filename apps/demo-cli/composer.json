{
  "name": "thorsten/dynamic-data-importer-demo-cli",
  "type": "project",
  "version": "0.9.0",
  "description": "Demo CLI application for the dynamic data importer.",
  "license": "MIT",
  "require": {
    "php": ">=8.4",
    "thorsten/dynamic-data-importer-core": "^0.9",
    "thorsten/dynamic-data-importer-cli-adapter": "^0.9"
  },
  "repositories": [
    {
      "type": "path",
      "url": "../../packages/core",
      "options": {
        "symlink": true
      }
    },
    {
      "type": "path",
      "url": "../../packages/cli-adapter",
      "options": {
        "symlink": true
      }
    }
  ],
  "bin": [
    "bin/import"
  ],
  "scripts": {
    "import": "php bin/import",
    "demo:analyze": "php bin/import analyze --file data/sample.csv",
    "demo:preview": "php bin/import preview --file data/sample.csv --map name=full_name --map age=years",
    "demo:execute": "php bin/import execute --file data/sample.csv --output-format json --write-output build/import-result.json"
  }
}
